#include "stdio.h"

int mac(int, int, int);

int main()
{
        int a = 3;
        int b = 5;
        int c = 200;

        int result = mac(a, b, c);
        printf("result = %d\n", result);

        return 0;
}
